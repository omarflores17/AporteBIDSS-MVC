@model ERPMVC.Models.ProformaInvoice
@using Microsoft.AspNetCore.Http;
@{
    ViewData["Title"] = "DetailsContado";
}

    <script>
        function GetInvoiceId() {
            return { ProformaId: $("#ProformaId").val() }
        }
    </script>

    <div id="ContratoDetail">
        @await Html.PartialAsync("pvwAddContado", Model)
    </div>

    <div class="container-fluid">

    </div>

<script>
    $(document).ready(function () {
        PrepararGrid();
    });

    
    function PrepararGrid() {
        //clearsession();
        var dataSource = $("#gridContadoDetalle").data("kendoGrid").dataSource;
        var raw = dataSource._data;
        console.log(raw);
        debugger;
        for (var i = dataSource._data.length - 1; i >= 0; i--) {
            item = raw[i];
            dataSource.remove(item);

    </script>
