@model ERPMVC.Models.ProformaInvoice
@using Microsoft.AspNetCore.Http;
@{
    ViewData["Title"] = "Facturación al Contado";
}



<script>
    function GetInvoiceId() {
        return { ProformaId: $("#ProformaId").val() }
    }



</script>


<div id="salesorder">
    @await Html.PartialAsync("pvwProformaInvoice", new ERPMVC.DTO.ProformaInvoiceDTO { ProformaId = Model.ProformaId, CustomerId = Model.CustomerId, OrderDate =DateTime.Now, DeliveryDate = Model.OrderDate, ExpirationDate = DateTime.Now.AddDays(30) })
</div>



<div class="container-fluid">

</div>
<script>
    $(document).ready(function () {
        PrepararGrid();    
    });

    function PrepararGrid() {
        //clearsession();
        var dataSource = $("#gridProformaInvoiceDetail").data("kendoGrid").dataSource;
        var raw = dataSource._data;
        console.log(raw);
        debugger;
        for (var i = dataSource._data.length - 1; i >= 0; i--) {
            item = raw[i];
            dataSource.remove(item);
        }
        // $('#ModalJournalEntry').find(':input').not(':submit').val('');
        // $('#ModalJournalEntry').modal('show');
        //$("#SalesOrderId").val('0');
    }
</script>